class Commission {
  Commission({this.raw, this.commission, this.tip, this.total, this.id, this.date});
  double raw;
  double commission;
  double tip;
  double total;
  DateTime date;
  String id;
}